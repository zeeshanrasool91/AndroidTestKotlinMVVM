package ae.android.test.networking.enums

enum class ErrorType {
    NETWORK_ERROR,  //Network error
    SERVICE_ERROR,  // Server access exception
    RESPONSE_ERROR,  // Request return value is abnormal
    GENERAL_ERROR // Request return value is abnormal
}